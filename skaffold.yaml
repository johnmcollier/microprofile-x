apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/{{.GITUSER}}/microprofilex:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/{{.GITUSER}}/microprofilex:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: microprofile
        chartPath: charts/microprofilex
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/{{.GITUSER}}/microprofilex"
          image.tag: "{{.DIGEST_HEX}}"
