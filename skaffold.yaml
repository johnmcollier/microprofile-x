apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/johnmcollier/microprofile:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/johnmcollier/microprofile:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: microprofile
        chartPath: charts/microprofile
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/johnmcollier/microprofile"
          image.tag: "{{.DIGEST_HEX}}"
